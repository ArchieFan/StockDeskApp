@model StockDesk.WebApp.ViewModels.PortfolioManagementFinishViewModel
@{
    ViewData["Title"] = "Finish Trading job";
}

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}

    <h3 id="PageTitle">Portfolio Management - finish trading job</h3>

<form asp-controller="PortfolioManagement" asp-action="FinishTrading" method="post">
    <div class="form-group">
        <label asp-for="ActualStartTime"></label>
        <input asp-for="ActualStartTime" type="text" class="form-control" />
        <span asp-validation-for="ActualStartTime" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="ActualEndTime"></label>
        <input asp-for="ActualEndTime" type="text" class="form-control" />
        <span asp-validation-for="ActualEndTime" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Notes"></label>
        <input asp-for="Notes" type="text" class="form-control" />
        <span asp-validation-for="Notes" class="text-danger"></span>
    </div>
    <input asp-for="Date" type="hidden" />
    <input asp-for="Id" type="hidden" />
    <span><button id="CompleteButton" type="submit" class="btn btn-default">Complete</button></span>
    <span><a id="BackButton" asp-action="Details" asp-controller="PortfolioManagement" asp-route-planningDate="@Model.Date.ToString("yyyy-MM-dd")" asp-route-jobId="@Model.Id" class="btn btn-default">Cancel</a></span>
</form>


